# Future native installer

This folder contains installer planning assets only. The final customer artifact will be `PicoDeGallo-Setup.exe`, but no installer binary is generated or committed in this phase.
